##Diagrama de Relacionamento

Clientes
---------------
id_cliente (PK)
razao_social
id_estado (FK) ----> Estados.id_estado (PK)

Telefones
---------------
id_telefone (PK)
numero
id_tipo_telefone (FK) ----> TiposDeTelefone.id_tipo_telefone (PK)
id_cliente (FK) ----> Clientes.id_cliente (PK)

TiposDeTelefone
---------------
id_tipo_telefone (PK)
descricao

Estados
---------------
id_estado (PK)
sigla
nome


##Comando SQL para Buscar Clientes em São Paulo

SELECT
    c.id_cliente,
    c.razao_social,
    t.numero AS telefone
FROM
    Clientes c
JOIN
    Estados e ON c.id_estado = e.id_estado
JOIN
    Telefones t ON c.id_cliente = t.id_cliente
WHERE
    e.sigla = 'SP';

